# 「生成祷告」修复 · 验收报告

状态：**代码已完成并本地验证；等待部署 Cloudflare Worker + API Key 后，跑 20 项真机测试补全本报告的第 3 节。**

---

## 1. 故障原因（为什么"总是马太福音 11:28"）

经查阅源码，根因是：**这个功能此前根本没有接入任何 AI。**

- 它是纯前端的**关键词 → 固定模板**查表：`data/prayerEngine.js` 里的 `getPrayerResponse()`（约第 400 行）先检测危机词，再在 **16 个固定主题**里做关键词子串匹配。
- **匹配不上就返回 `getGeneralPrayer()`，而它被硬编码为《马太福音》11:28**（`prayerEngine.js` 约第 329 行）。用户写的自由问题只要没命中那 16 个关键词，就全部落到这个兜底 → 永远是 11:28。
- 即使命中主题，返回的也是**该主题预先写好的整段模板**，不是根据用户这句话生成的。
- **全链路没有任何网络请求 / API / 缓存**（`components/prayer-care.js`、`prayerEngine.js` 全程同步）。因此原怀疑清单中「用户输入是否传入 AI 请求 / 提示词把 11:28 设为默认 / 固定数据库返回 / API 失败回退固定答案 / 复用第一次生成 / 提示词缓存」——**均不适用，因为当时不存在 AI 请求与提示词**。上一个提交 `0922b4d` 只是把主题从 8 个加到 16 个，缓解但未根治。

**结论**：这不是"把固定答案改成随机答案"能解决的，而是需要**接入真正的大模型**：理解用户处境 → 从整本圣经 66 卷匹配相关经文 → 解释 → 生成个人化祷告。

---

## 2. 修改内容

新增一个**服务端代理（Cloudflare Worker）**调用 Claude，前端改为调用它；纯静态站因此无需暴露 API Key。

| 文件 | 作用 |
|---|---|
| `worker/prayer-worker.js` | **新增**。Cloudflare Worker：接收 `{input, lang}`，用严格的系统提示词调用 Anthropic Claude（默认 `claude-opus-5`），要求按**五段结构**输出、整本圣经匹配、经文准确（中文=和合本、英文=WEB 公有领域）、危机安全规则、不编造经文、温暖不责备，返回严格 JSON。**API 失败一律返回错误码，绝不返回伪造成功答案。** |
| `worker/wrangler.toml` | **新增**。Worker 配置（模型/effort 变量）。 |
| `data/prayerConfig.js` | **新增**。存放 Worker 网址；留空时前端显示"正在开通中"，不显示假答案。 |
| `data/prayerClient.js` | **新增**。前端请求封装：POST 到 Worker，失败抛出带 `code` 的错误，供 UI 显示明确失败。 |
| `data/prayerEngine.js` | 新增导出 `getPrayerCrisis()`（仅复用既有危机检测），供前端**离线**即时显示 988；旧模板引擎不再进入正式祷告路径。 |
| `components/prayer-care.js` | 重写正式祷告流程：① 危机词**离线即时**显示 988/911；② Worker 未配置 → 诚实"开通中"；③ 否则显示加载动画 → 调用 AI → 渲染**五段祷告板**（回应/相关经文/为什么是这些经文/祷告/温柔下一步）；④ 失败 → 明确的"这次没能生成祷告"+ 重试，**绝不回退到固定经文**。新增可朗读祷告的 TTS 按钮。 |
| `PRAYER_AI_SETUP.md` | **新增**。部署指南（Cloudflare 后台粘贴 / wrangler、设 Key、费用与模型选择、限流防滥用、排查）。 |
| `scripts/test-prayer.mjs` | **新增**。20 主题自动验收脚本。 |

### 满足的要求核对
- ✅ 以整本圣经 66 卷为知识范围，按问题含义匹配，非固定/非随机（系统提示词强制）。
- ✅ 五段结构：理解与回应 / 相关经文（书卷·章节·译本）/ 简短解释 / 个人化祷告 / 实际鼓励。
- ✅ 经文准确、标注译本；中文和合本、英文 WEB（公有领域，规避版权）；要求不编造、不混译本。
- ✅ 语气温暖真诚、不机械、不责备；不宣称代表上帝、不保证治愈/困难消失。
- ✅ 危机（自杀/自残/暴力/虐待/医疗急症）：前端离线即时 988/911 + 提示联系专业与可信之人；模型侧亦有安全兜底；祷告不代替现实援助。
- ✅ 中英文分别以对应语言作答；切换语言会以新语言重新生成。
- ✅ API 失败明确提示失败，**不显示伪造成功答案**，也不再返回固定 11:28。

### 本地已验证（无需 Key 的部分）
- ✅ 五段祷告板（中/英）渲染正确、配色与网站一致、可读。
- ✅ 危机板安全区块（988/911）置顶显著。
- ✅ 未配置端点 → 显示"正在开通中"（非假答案）。
- ✅ 危机词离线即时 988，不走网络、不花钱。
- ✅ `index.html` 等页面无 console 报错；圣经问答（biblical-qa）未被破坏。

---

## 3. 20 项真机测试结果（部署后补全）

部署 Worker 并在 `data/prayerConfig.js` 填好网址后，运行：
```bash
node scripts/test-prayer.mjs <worker 网址>
```
将自动核对 20 个不同主题（焦虑/疾病/婚姻/悔改/丧亲/方向/感恩/孤独/经济/为孩子代祷/成瘾…含 2 个危机主题，中英各半），并断言：

- [ ] 每个回答都切合问题（经文与处境相关）
- [ ] 经文出处准确（书卷、章节、译本齐全）
- [ ] 包含完整祷告文
- [ ] **不再总是返回《马太福音》11:28**（脚本统计首选经文的多样性与 11:28 出现次数）
- [ ] 危机主题识别并包含 988
- [ ] 手机端 / 电脑端结果都正常（响应式已本地验证；真机再确认）
- [ ] API 失败时明确提示失败，不显示伪造成功答案

> 结果会写入 `scripts/prayer-test-results.json`。把它发我，我据此填好上面的勾选并附每题的经文与祷告摘要，交你最终确认。

---

## 4. 后续（可选，非本次必须）
- 圣经问答页（`biblical-qa.js`）里的"祷告拦截"目前仍用旧模板引擎，可用同一个 Worker 客户端迁移，行为即与主祷告面板一致。
- 若要进一步保证经文**逐字**无误，可在 Worker 内加一层"经文核对"（对照本地公有领域经文库校验引用），作为二期增强。
