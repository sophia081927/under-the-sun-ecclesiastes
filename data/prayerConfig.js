/* prayerConfig.js — where the deployed prayer Worker lives.
   ------------------------------------------------------------
   After deploying the Cloudflare Worker (see PRAYER_AI_SETUP.md), paste its
   public URL between the quotes below, e.g.
       export const PRAYER_API_ENDPOINT = 'https://lol-prayer.YOURNAME.workers.dev';

   Leave it EMPTY ('') until the Worker is live. While empty, the prayer panel
   shows an honest "being switched on" notice — never a fake or canned answer. */
export const PRAYER_API_ENDPOINT = '';
