# PPT 与祷告功能交接

本次修复已完成离线验证，未部署到生产网站。

## 已修复

- 主祷告入口和人生问答中的祷告请求统一调用 AI 服务。未配置时两个入口均明确提示未开通，不再调用旧模板。
- 中英文切换重新请求对应语言，取消过期请求；失败可重试。
- 前端 50 秒超时；上游连接 45 秒超时。输入最多 1000 字符。
- 校验完整回应结构、译本标签及引用格式。拒绝缺段落、空经文、截断回复及不完整的危机提示。
- 拒绝不在允许列表中的浏览器来源，不向前端回传上游内部错误。
- 中文输入法选词时按 Enter 不提交；输入框增加可访问名称。

## 验证范围

8 项 Node 离线服务测试通过。浏览器隔离测试通过：两个未配置入口、两个 AI 入口、中英文切换、错误无模板回退、重试、离线危机入口、无组件异常。浏览器使用模拟响应，不能证明真实模型的经文准确性和相关性。

## 尚未完成，不能标成正式上线

1. 部署 Cloudflare Worker，配置 ANTHROPIC_API_KEY 服务端密钥。
2. 在 data/prayerConfig.js 填写真实公开端点，并发布网站改动。
3. 对真实端点运行 20 主题测试；人工核对经文逐字准确性、处境相关性和双语质量。
4. 当前经文原文仍由模型输出，尚无全本圣经逐字校验数据库。结构检查不是引文真实性保证，必须在正式验收时单独处理。

## 部署方式

推荐从 worker 目录用 Wrangler 部署，入口为 prayer-worker.js，它会包含共享校验模块。
若使用 Cloudflare 网页编辑器，先运行 `node scripts/build-prayer-worker.mjs`，粘贴生成的 `worker/prayer-worker-standalone.js`，不能只粘贴带 import 的源文件。
密钥只在 Cloudflare 后台设置，不发在聊天里。

## PPT

中英文各 13 页，Reviewed 版更新第 9 页的接入状态，英文第 8 页纠正祷告示例。保持原图像型 PPT 格式；大部分文字无法在 PowerPoint 中逐字编辑。网站链接保留，文件结构与尺寸检查通过。
PPT 修订使用内置 ImageGen 工具。提示要求保留金色布局与其他内容，仅修改第 9 页状态文字及英文第 8 页示例。具体图片与构建文件保存在当前 Codex 工作目录。

Claude Code 的直接核实请求因 ConnectionRefused 未成功；本文件供其后续继续工作。
